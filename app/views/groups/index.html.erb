<main>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary justify-content-between fixed-top">
<div class="hamburger-icon">
  <span></span>
  <span></span>
  <span></span>
</div>
<div class="container">
<h3 class="m-0 p-0 white-color">Create New Category</h3>
  <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "text-white link-tag"%>
</div>

</nav>
  
   <section class="space-top">
      <% @group.each do |item|%>
       <div class="d-flex flex-direction-row justify-content-between bg-white text-align-center align-content-center border border-grey p-1 m-4 mb-5">
         <img src=<%= item.icon %> class="img-boostrap">
         <div class="group-text">
           <h4 class="link-tag">
             <%= link_to item.name, new_user_group_budget_path(current_user,item), class: "link-tag" %>
            </h4> 
            <p class="boostrap-text"><%= item.created_at.to_date %></p>
         </div>
         <div class="boostrap-class">$<%= item.total_amount_by_group(item.id)%></div>
      </div>
      <% end %>
   </section>

<section class="d-flex flex-column fixed-bottom text-center bg-green w-100 p-2">
  <h2>
    <%= link_to 'Add New Category', new_user_group_path, class: "btn btn-primary btn-lg mt-3 link-tag"%>
  </h2>
</section>


</main>